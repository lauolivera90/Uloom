# Uloom — Contexto para agentes de desarrollo

Aplicación de escritorio para Windows, Electron + Vite + React (JS puro, SIN TypeScript) + Tailwind CSS. Gestor/lanzador de Workspaces basado en Web Tabs.

## Antes de escribir código

Leé siempre, en este orden, antes de tocar cualquier archivo:

1. `.doc/architecture.md` — estructura de carpetas (FSD simplificado), capas del backend (ipc/services/data), convenciones de nomenclatura.
2. `.doc/rules.md` — reglas obligatorias: separación lógica/hooks, memory leaks, barrels, manejo de errores IPC, JSDoc.
3. `.doc/design.md` — sistema de diseño: fuente de verdad de tokens (`src/renderer/app/index.css` + mapeo en `tailwind.config.js`), lenguaje visual (shape, bordes, sombras, focus, motion, iconos) y convenciones de widgets. Regla actual: los componentes usan SOLO tokens (`bg-background`, `text-text`, `bg-primary`, `text-on-primary`, `bg-surface`, `border-border`, `duration-fast`, etc.) — nunca hex hardcodeados ni colores de la paleta por defecto. Seguí los patrones de los widgets existentes (ej. `Button` y sus `variants`) y consumí SIEMPRE desde el barrel de `widgets`.

**Regla general de scope:** implementá únicamente lo que está descrito en `.doc/architecture.md` y `.doc/rules.md` para la fase actual (ver abajo). Si te parece que hace falta algo que no está documentado ahí, preguntame antes de darlo por sentado — no asumas ni agregues funcionalidad no pedida, aunque te parezca una buena idea.

## Fase actual: v0.5.3

3 páginas: Hub de Sesiones, Detalle de Sesión, Configuración. Navegación principal con Sidebar colapsable (modelo push, persistencia en localStorage, item activo por ruta), que incorpora el botón directo "Agregar sesión" (siempre usable, `GlobalCreateWorkspace`). La **portabilidad quedó completa**: exportar (sesión individual en el Detalle y respaldo total, ambos `.json` con diálogo nativo) e **importar** (Configuración → Sesiones, diálogo de apertura nativo). El **tema es funcional** (toggle con runtime: `ThemeProvider` + `useTheme`, persistencia en `localStorage['uloom-theme']`, default al `prefers-color-scheme` y anti-flash) y desde v0.5.2 la **paleta de identidad es selectable** (5 paletas predefinidas con `data-palette` en CSS, persistencia en `localStorage['uloom-palette']`, anti-flash con allowlist y selector de swatches en Configuración). La ventana tiene **tamaño real con mínimos** (1100×750, min 800×600) y **sin menú nativo** (`autoHideMenuBar`); se eliminó el DevTools automático. Las tabs de Configuración (Preferencias con navegador por ícono efectivo y Sesiones con importar/exportar todo/borrar caché/eliminar todas) son funcionales, con **búsqueda** en el header que filtra todas las opciones. El **Motor de Lanzamiento sigue operativo**. El **idioma es selectable** (es/en, adelanto de v0.5.1): `LanguageProvider` + `useI18n` con persistencia en `localStorage['uloom-language']`, default por `navigator.language` y selector en Configuración → Preferencias. El Detalle ganó un **grid definido y consistente** (2 columnas desde `lg`: izquierda absorbente + derecha fija en `32rem` desde `lg` y `40rem` desde `xl`, stack por debajo) y los headers de modal/ConfirmDialog usan **accent** como las cards; la app pasó una **auditoría general** (consolas, lints y comportamientos) sin hallazgos pendientes. Sistema de tokens con la regla "fill vs foreground" (`primary` = fill, `primary-hover` = foreground), `accent` para identidad no interactiva y **tokens de identidad palette-scoped** (ver `.doc/design.md`). La **duplicación de sesiones** (v0.5.3) está operativa: botón en el Detalle → modal precargado con los datos de la fuente (nombre/icono/descripción editables antes de confirmar) → clonado en el backend vía `workspace:duplicate` (id y tabs nuevos, configuración de lanzamiento copiada, fuente intacta), con nombre sugerido de semántica `root (count)`. La hoja de ruta por versión vive en `Plan/to_do.md`.

**Nuevo en v0.5.3 — Duplicar sesión y feedback de alta:** botón `content_copy` (`IconButton` ghost) en el header del Detalle → **modal precargado** con los datos de la fuente (mismo `WorkspaceFormModal`, modo `isDuplicating`: título/confirmar "Duplicar sesión"/"Duplicar") donde nombre/icono/descripción se pueden cambiar antes de confirmar (los tabs se editan después, como siempre). El clonado vive en el **backend**: canal `workspace:duplicate` → `workspaceService.duplicateWorkspace(sourceId, input)` lee la fuente con `getWorkspaceById`, arma un workspace nuevo (`id` randomUUID) con las **tabs clonadas con ids nuevos** y `openBehavior`/`browser` copiados — la original nunca se toca. En el renderer, `useWorkspaceState.duplicateWorkspace` (patrón de create) + hook de feature `useDuplicateWorkspace` (prefill `{...workspace, name: inferDuplicateName(...)}`, submit → `duplicateWorkspace(source.id, data)`). **Nombre sugerido** con `shared/lib/workspaceName.js` → `inferDuplicateName(name, existingNames)`: semántica `root (count)` — si el nombre ya termina en ` (N)` se descarta el sufijo, el resultado es el root + cantidad de sesiones que matchean `root` o `root (N)`; **nunca apila** (`hola (1)(1)` jamás) ni colisiona (`hola` → `hola (1)` → `hola (2)`). **Feedback visual** (regla 10): duplicar tostéa éxito (`duplicate.success`) porque el resultado (copia en el Hub) no es visible desde el Detalle; además el alta global del Sidebar pasó a un hook (`app/hook/useGlobalCreateWorkspace`) que **tostéa `create.success` solo fuera del Hub** (`pathname !== '/'`; en el Hub la card es visible in-place → sin toast, igual que el alta del Hub). Sin toast de error propio: `useWorkspaceFormModal` cubre con `save.error`. i18n: 5 claves nuevas es/en con paridad. La política de toasts quedó actualizada en `.doc/design.md` §3 y `.doc/rules.md` §10 (casos `duplicate` y `create-global`).

**Nuevo en v0.5.2 — Temas personalizados (paletas de identidad):** la app pasa de claro/oscuro a **modo × paleta**. Los tokens de identidad (`primary`, `primary-hover`, `accent`, `on-primary`) se redefinen por paleta vía el atributo `data-palette` en `src/renderer/app/index.css` (bloques `[data-palette='<id>']` claro y `.dark[data-palette='<id>']` oscuro; los estructurales siguen en `:root`/`.dark`). El atributo funciona en el `<html>` (paleta activa) o en cualquier subárbol (preview en vivo de un swatch — el widget `PalettePicker` usa esto para mostrar cada paleta con tokens puros, sin duplicar colores). **5 paletas predefinidas**: `violeta` (default, valores actuales), `azul`, `esmeralda`, `ambar`, `rosa`, con contraste AA verificado (≥4.5:1 en `on/fill`, `hover/bg`, `accent/bg` en ambos modos). Catálogo en `shared/lib/palettes.js` (ids + `labelKey`, **sin colores** — CSS es la fuente; `normalizePalette` con fallback al default) y `useTheme` ahora expone `{ theme, setTheme, toggleTheme, palette, setPalette }` (persistencia en `uloom-palette`, seed desde `documentElement.dataset.palette`). El **anti-flash** (`public/theme-init.js`) setea `data-palette` antes del paint validando contra un allowlist que **debe sincronizarse a mano** con el catálogo al agregar paletas (⚠️ documentado). Selector en Configuración → Preferencias: fila "Paleta de colores" con el widget `PalettePicker` (grilla de swatches `w-7 h-7`, preview partido primary/accent, radio group con `aria-label`/`title`, labels por `t(palette.labelKey)`). Cambio de paleta in-place y visible → sin toast (regla 10). i18n: 7 claves nuevas es/en con paridad. Fuera de esta fase: v0.5.3 (duplicar sesión), v0.5.4 (historial de tabs), personalización manual de colores y quick wins del Hub (v0.6.x).

**Nuevo en v0.4.5 — Responsive y sidebar superpuesta:** la app se adapta a anchos menores sin rediseño. El **Hub** muestra 3 columnas desde `lg` (`sm:grid-cols-2 lg:grid-cols-3`); el Detalle mantiene su grid (ya stackeaba bajo `lg`) y Configuración no necesitó cambios. Lo central: en pantallas **pequeñas (< 1024px, breakpoint `lg`)** la sidebar queda SIEMPRE colapsada y **fija al borde izquierdo** (`fixed inset-y-0 left-0 z-[40]`, debajo de modales/toasts): al hacer **hover se expande SUPERPUESTA** sobre el contenido (`w-56`, `shadow-xl`) sin quitarle espacio, y vuelve a colapsar al salir el puntero; `MainLayout` reserva solo el rail colapsado con `pl-16` en `main`, y el botón de colapsar se oculta en overlay (la preferencia persistida `ui.sidebarCollapsed` solo aplica ≥`lg`). Infraestructura: nuevo hook genérico `shared/hook/useMediaQuery` (implementado con `useSyncExternalStore` + listener `change` de `matchMedia` con cleanup, regla 2.2) y `useSidebar` ahora devuelve `{ collapsed, toggle, isSmall }`. El estado `hovered` vive en `Sidebar.jsx` (hover trivial, excepción regla 1); `SidebarItem` gana `aria-label` en estado colapsado (a11y). Responsive documentado en `.doc/design.md` §3 (bullet del widget Sidebar + grid del Detalle sincronizado al código real). Fuera de esta fase: v0.5.x (temas personalizados, duplicar sesión, historial de tabs) y quick wins del Hub (v0.6.x).

**Nuevo en v0.4.4 — Feedback visual (toasts y errores de importación):** nace el **sistema de toast** y se aplica en toda acción de usuario que antes solo logueaba con `console.error`. La infraestructura sigue el patrón de i18n: contexto `shared/hook/useToast` (`ToastContext` + `useToast` → `{ toast({ variant, message, duration? }), dismiss(id) }`), estado + timers en `app/hook/useToastState` (auto-cierre 4s, tope 4 visibles con cleanup de timers, regla 2.2) y `app/ToastProvider` (value memoizado, renderiza el viewport; cableado en `App.jsx` dentro de `LanguageProvider` y fuera de `WorkspaceProvider`). Los widgets `Toast`/`ToastViewport` (barrel `widgets`) son presentacionales: viewport fijo `bottom-6 right-6 z-[60]`, variantes solo tokens — `success` = fill `primary` (confirmación = acción), `error` = `error`, `warning` = `tertiary`, `info` = superficie — ícono Material Symbols líder y close con `aria-label` (`toast.dismiss`); el botón de cierre sobre fills es un bespoke documentado en `.doc/design.md` §3. La **importación** gana **control de errores por caso**: `PORTABILITY_ERROR_CODES` (INVALID_JSON, NOT_ULOOLM_FILE, UNSUPPORTED_KIND, INVALID_SCHEMA_VERSION, INVALID_WORKSPACES, READ_ERROR, PERSIST_ERROR) adjuntados en `portabilityService`, propagados por `portabilityHandler` (`code` en la respuesta IPC) y adjuntados al throw por `portabilityIpcApi`; `usePortability` mapea `code → clave i18n` (fallback genérico) y confirma el éxito con conteo (`import.success`, plural). **Toasts aplicados**: import, exportAll, clearCache, deleteAll, delete tab/session (`useConfirmAction` gana `errorKey`), launch (error + `launch.partialFailure`), export sesión, forms (`useWorkspaceFormModal`/`useTabModal`), navegador predeterminado y load inicial. Sin toast (soft-fallback interno): `useTabForm` metadata, `useCachedQuery`, `preload.js`; `useSessionConfig` mantiene su error inline en `WorkspaceConfig`. `useWorkspaceState.importWorkspaces` devuelve `{ canceled, imported }`. Los `console.error` quedan como log dev (no se traducen, regla 9); los toasts usan claves i18n nuevas (22 por idioma con paridad completa). Post-cierre: el **Motor de Lanzamiento** abre el conjunto de pestañas en **un solo spawn** (`openUrlsInBrowser` con todas las URLs — `new-window` abre el conjunto en una sola ventana, cada URL como pestaña; `active-tab` como pestañas en la ventana vigente; un spawn fallido total → `launch.error`, y `launch.partialFailure` solo puede venir del fallback `shell.openExternal`), y la **política de toasts** quedó documentada como regla obligatoria en `.doc/rules.md` §10 (principio en `.doc/design.md` §3: solo toast si el resultado no es visible en la vista actual). Fuera de esta fase: responsive parcial y sidebar superpuesta (v0.4.5).

**Nuevo en v0.4.3 — Pase de UI y estabilidad:** el **grid del Detalle de Sesión** deja de usar un ancho fijo de `40rem` que aplastaba la lista de tabs: ahora es `grid grid-cols-1 lg:grid-cols-[minmax(0,1fr)_24rem] items-start gap-6` — izquierda absorbente (`minmax(0,1fr)`), derecha fija en `24rem` (cards Configuración/Exportar) y stack en una columna por debajo de `lg`; la regla de decisión de anchos quedó documentada en `.doc/design.md`. Los **headers de modal y confirm** (`Modal`, y por herencia `ConfirmDialog`) cambian el título de `text-text` a `text-accent`, el mismo tratamiento visual que los headers de card del Detalle (`ResourceCardHeader`). La **auditoría general** recorrió renderer y main (hooks de estado/form/modal, widgets, entidades, features, IPC, services y repositories): `npm run lint` limpio, sin errores de consola en los flujos normales (los `console.error` restantes son la convención de fallback, sin toasts — v0.4.4), sin unhandled rejections (todo encadenado con `.catch` en el líder de escritura `useWorkspaceState`) y con todos los efectos usando cancel flags/cleanup. Fuera de esta fase: feedback visual/toasts (v0.4.4) y responsive parcial (v0.4.5).

**Nuevo en v0.4.3 — Idioma es/en (adelanto de v0.5.1):** los textos visibles dejaron de estar hardcodeados: viven como claves en los diccionarios `shared/lib/i18n/{es,en}.js` (única fuente de verdad) y los componentes los resuelven con `useI18n()` → `t(clave, { params })` (plurales `{one,other}` e interpolación `{param}`; fallback a la clave). `LanguageProvider` + `app/hook/useLanguage` proveen `{ language, t, setLanguage }` con persistencia en `localStorage['uloom-language']`, default por `navigator.language`, `documentElement.lang` sincronizado y selector "Idioma" en Configuración → Preferencias. El contexto y `useI18n` viven en `shared/` para que widgets y features lo consuman sin ciclos de capas; `workspaceLabels.js`/`workspaceLaunch.js` exportan claves y `buildOpenBehaviors(t)` arma las opciones del Select. Regla completa en `.doc/rules.md` §9.

**Nuevo en v0.4.2 — Importación, temas runtime y shell de la ventana:** la **importación** real reemplaza el placeholder de Configuración → Sesiones: el canal `portability:import` (`portabilityService.importFromFile` + `importWorkspaces` en `workspaceRepository`) abre el diálogo nativo de apertura (cancelar no es error), valida el wrapper exportado (`app: 'uloom'`, `kind`, `schemaVersion` string, `data` con workspaces) y reconstruye el catálogo con **semántica por kind**: `workspace` agrega la sesión (id colisionante → randomUUID) y `backup` reemplaza el catálogo preservando `preferences`. `useWorkspaceState.importWorkspaces` se serializa en el líder de escritura y rehidrata el estado con la lista persistida (`hydrateCatalog` compartido con el load inicial); `usePortability` + botón Importar cableado con `disabled` durante el proceso. El **tema** deja de ser maqueta: `src/theme-init.js` (script clásico en el `<head>`, respeta CSP `script-src 'self'`) aplica `.dark` antes del paint desde `uloom-theme` o el `prefers-color-scheme`; `app/ThemeProvider` + `app/hook/useTheme` exponen `{ theme, setTheme, toggleTheme }` con persistencia en `localStorage['uloom-theme']`, y `useSettings` consume `useTheme` (se eliminó la maqueta local). La **ventana** (`src/main.js`) pasa a `1100×750` con `minWidth: 800`/`minHeight: 600` y `autoHideMenuBar: true` (menú oculto, reaparece con Alt) y se eliminó el `openDevTools()` automático. Fuera de esta fase: no queda pendiente conocido del MVP; próximas ideas en `Plan/to_do.md`.

**Nuevo en v0.4.1 — Portabilidad (exportación), limpieza y UX:** la exportación real reemplaza los placeholders de Configuración: **exportar una sesión** (Detalle → card "Exportar", `WorkspaceExportCard` + `useExportWorkspace`) y **exportar todo** (Configuración → Sesiones), vía los canales `portability:exportWorkspace`/`portability:exportAll` (`portabilityService` + `portabilityIpcApi`) con wrapper versionado (`kind: workspace|backup`, `schemaVersion '0.4.1'`) y diálogo nativo donde cancelar no es un error. **Limpieza en Configuración → Sesiones**: "Borrar caché" (`workspace:clearMetadataCache`, remueve `Tab.favicon`) y "Eliminar todas las sesiones" (`workspace:clearAll`, preserva `preferences`) con doble confirmación (aviso + confirmación pesimista que solo cierra ante éxito). **Sidebar**: botón directo "Agregar sesión" (`GlobalCreateWorkspace` en `app/`, siempre usable). **Configuración**: búsqueda en el header que filtra todas las opciones (data-driven `allOptions`). Pase del @reviewer: `useConfirmAction` (genérico en `shared/hook`) y `useWorkspaceFormModal` (genérico en `entities/workspace/hook`) consolidan 3 máquinas de confirmación → 1 y 3 modales de form de sesión → 1 (se eliminó `useCreateWorkspaceModal`).

**Nuevo en v0.3.2 — Pase de UI (selectores, sesiones vacías e íconos de navegador):** los modales de sesión y pestaña usan el **mismo selector de ícono colapsado** — widget `IconPickerField` (tile de preview + botón «Subir icono» disabled / «o» / «Elegir uno») que lleva la lógica en `useIconPicker` (`shared/hook`, `showPicker`/`toggleShowPicker` centralizados) y `previewIcon` en `useWorkspaceForm`. Una sesión **sin pestañas ya no se puede lanzar**: la card del Hub muestra `(+)` (`onAddTab`) y el header del Detalle "Agregar pestaña"; `TabFormModal`/`useTabForm`/`useTabModal` viven en `entities/workspace` (precedente v0.2.4) porque los consumen Hub y Detalle, con `useTabModal` inyectando `addTab`/`updateTab`; con pestañas quedan el play/"Lanzar". El **navegador efectivo se muestra con su ícono oficial** junto a cada Select: 6 SVGs de browser-logos empaquetados en `entities/workspace/assets/browsers/`, mapa `browserIcons.js` y widget `BrowserIcon` (fallback glifo `public`), en la card Configuración del Detalle (`WorkspaceConfig` con `useSessionConfig.resolvedBrowserId`) y en Configuración → Preferencias (`system` → `systemDefaultId`). Para el caso "Predeterminado" el id real del SO se resuelve por el **canal `browser:system`** (`browserService.getSystemDefaultBrowser` vía `resolveSystemBrowser` centralizado; `useSystemDefaultBrowser` con caché de módulo y `systemDefaultName` para el label). Pase del @reviewer: `useCachedQuery` (genérico en `shared/hook`) elimina el boilerplate de fetch/caché de `useInstalledBrowsers` y `useSystemDefaultBrowser`, y `browserService.resolveSystemBrowser` unifica la resolución de `app.getApplicationInfoForProtocol` que duplicaban el launcher y la UI. Fuera de esta fase: portabilidad/temas (v0.4).

**Nuevo en v0.2.4 — Auto-metadatos web + edición/borrado de sesión:** los modales de sesión y pestaña migraron a `entities/workspace/` como `WorkspaceFormModal`/`useWorkspaceForm` y `TabFormModal`/`useTabForm`, con reuso para alta/edición (props `initialWorkspace`/`initialTab`). El formulario de pestañas obtiene `<title>` y favicon reales del sitio vía el canal nuevo `page:metadata` (`pageService` con `net.fetch`, regex + AbortController, devuelve el favicon como data URL; CSP de `index.html` ampliado a `img-src 'self' data: https:`); la sugerencia de nombre va al placeholder con botón "Usar nombre sugerido". El ícono de pestaña se puede **restablecer** (default `autoFavicon ?? 'public'`) y la edición de pestaña conserva `tab.id`. En el backend, `workspace:delete` (`workspaceRepository.deleteWorkspace`) permite **borrar la sesión** desde el Hub y el Detalle con ConfirmDialog; en el renderer `useWorkspaceState` serializa el delete en `writeChainRef` y limpia `latestByWorkspaceRef` (mismo líder de escritura que `mutateWorkspace`). Bugfix: `useIconPicker` pasó a gancho controlado con `reset()` en `open()`. `Tab.favicon` ahora soporta símbolo | data URL | http(s) y el servicio de metadatos vive en la subcarpeta `services/lib/`.

**Nuevo en v0.2.3 — Refactor y deuda técnica:** versión sin features nuevas. El backend quedó **100% por dominio**: `data/` con `configStore` (acceso al archivo) + `workspaceRepository` (entidad) + `preferencesRepository`; `services/` con `workspaceService`/`browserService`/`preferencesService`; `ipc/` orquestado por handlers por dominio (`workspaceHandler` registra `config:get`, `workspace:create`, `workspace:update`). El renderer usa un **líder único de escritura**: `useWorkspaceState.mutateWorkspace(id, mutator)` serializa toda mutación de un workspace (pestañas + configuración por sesión) sobre el último persistido por id — elimina la raza cross-feature de v0.2.2; `useSessionConfig` ya no tiene cola propia. Deuda de widgets pagada: `ModalFooter` (par cancelar/confirmar de modales), `useIconPicker` en `shared/hook` (genérico), a11y de `FormField` (`required` propagado al control) y `@typedef` nombrados de formularios (`CreateWorkspaceFormState`, `AddTabFormState`). Fuera de esta fase: Launch real (v0.3), portabilidad/temas (v0.4).

**Nuevo en v0.2.1 — Tabs CRUD con persistencia real:** la app dejó de ser mock en memoria. Lee y escribe `config.json` vía canales IPC `workspace:create` y `workspace:update` (escritura **pesimista**; update estricto sin upsert; id de sesión por `crypto.randomUUID` en el service; cada mutación de pestañas reescribe el workspace completo; `tabs` normalizado a array en disco). El Detalle de Sesión (Lienzo) tiene el gestor de pestañas operativo: alta con `AddTabModal` (URL/nombre/ícono + favicon) y baja con `ConfirmDialog`. Los errores de persistencia hoy solo se loguean con `console.error`; el sistema de toast/mensajes se diseña casi al final del proyecto.

**Nuevo en v0.2.2 — Configuración de Navegador por Sesión:** la card Configuración del Lienzo dejó de ser placeholder. Define `openBehavior` (Ventana activa/nueva, default `active-tab`) y `browser` por sesión, con herencia de 3 niveles para el lanzamiento (v0.3): **sesión → global → sistema** (`sesión.browser ?? (global !== 'system' ? global : null)`). Solo se ofrecen navegadores **instalados** detectados por probe de rutas (`browser:list` → `browserService`, solo win32). `preferences.defaultBrowser` es una preferencia global funcional vía `config:updatePreferences` (merge parcial); escritura pesimista inmediata. Bordó la separación por dominio backend que se completó en v0.2.3.

Este proyecto está planeado para escalar en fases futuras. Esta sección se actualiza en cada fase nueva — no asumas que el scope de v0.4.5 es el scope final del proyecto, pero tampoco adelantes funcionalidad de fases futuras sin que se documente acá primero.

## Reglas no negociables (resumen — el detalle completo está en .doc/rules.md)

- Componentes `.jsx` solo presentan. Toda lógica, estado, efectos → hooks `.js` en `hook/`.
- Todo `useEffect` con timers/listeners/recursos necesita cleanup. Async en efectos necesita flag de cancelación.
- `useCallback` en props solo tiene sentido si el hijo está memoizado con `React.memo` — si no, es ruido.
- Barrels (`index.js`) con exports nombrados únicamente, sin lógica propia. Los imports entre capas siempre entran por el barrel, nunca apuntando directo a un archivo interno.
- Errores IPC: `data/` lanza, `services/` deja subir salvo fallback, `ipc/` SIEMPRE atrapa y responde `{ success, data, error }`, `[x]IpcApi.js` convierte `{success:false}` en `throw`.
- `@typedef` de entidades (`Workspace`, `Tab`) centralizados en un solo archivo, nunca redefinidos por archivo.
- Toda función exportada en services/repository/handlers/API de renderer necesita JSDoc con `@param`/`@returns`.

## Sincronización de documentación

Si tocás algo en `src/main/data/`, `src/main/services/`, `src/main/ipc/`, `src/preload.js`, o `entities/workspace/api/`, actualizá `.doc/backend.md` y/o `.doc/config_file.md` en el mismo cambio. No dejes código sin su doc correspondiente.

## Inicio / Desarrollo / Cierre de versión (checklist obligatorio)

**Al iniciar una versión nueva:**
1. Leer `changelog/changes_<versión-previa>.md` (o el de la versión en curso si quedó abierto) y `Plan/to_do.md`, para arrancar con el punto de partida real y no repetir trabajo ya hecho.

**Durante el desarrollo — solo a pedido del usuario:**
2. Cuando el usuario lo indique, crear/actualizar `changelog/changes_<version>.md` como memoria de sesión (lo hecho, lo pendiente, el estado actual) para que la próxima sesión retome donde se dejó. No se genera automáticamente: lo dispara el usuario.

**Al confirmar que una versión está terminada (lo indica el usuario), en el mismo cambio:**
3. Unificar la versión en `package.json`, `package-lock.json` (raíz), `src/main/data/configStore.js` (`APP_VERSION`), `.doc/backend.md`, `.doc/config_file.md` (header + defaults), `.doc/architecture.md` (header).
4. Completar `changelog/changes_<version>.md` con lo realmente shippeado (no lo que "debería" estar).
5. Verificar `Plan/to_do.md` contra el código: destildar (`[ ]`) cualquier item sin implementación real — la roadmap no debe sobre-registrar.
6. Actualizar `AGENTS.md`: la sección "Fase actual" a la nueva versión con su scope real.
7. Actualizar `.doc/backend.md`/`.doc/config_file.md` si el cierre tocó IPC/services/repository (regla 6 de `rules.md`).

## Flujo de trabajo con agentes

- **Plan** (agente primario): usalo para decisiones de arquitectura o cuando quiera pensar en voz alta sin que se toque ningún archivo todavía.
- **Build** (este agente, primario): implementación real, una vez que la decisión ya está tomada.
- **@reviewer** (subagente, solo lectura): auditoría de componentes/hooks contra `rules.md` (separación lógica/UI, simplicidad de pages, duplicación). Invocalo después de crear o modificar un componente — no lo uses para escribir código, solo para señalar problemas.
- **@explore** (subagente, solo lectura): para entender partes del código antes de tocarlas, sin gastar contexto de la sesión principal.

## Convención de sesiones

Una sesión por unidad de trabajo (un feature, un bugfix, una revisión), no una sesión única para todo el proyecto. Este archivo se recarga solo al empezar cada sesión nueva — no hace falta que le vuelva a pegar `architecture.md` o `rules.md` a mano.